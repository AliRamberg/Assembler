#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <stdio.h>
#include "line.h"

int 
parse_line(line_t *line)
{
    int args;
    /* args = sscanf(line->line, "%s", line->first); */
    /* printf("first = %s", line->first); */
    return args;
}